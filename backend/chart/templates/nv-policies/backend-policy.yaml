{{- if .Values.neuvector.enabled }}
apiVersion: neuvector.com/v1
kind: NvSecurityRule
metadata:
  name: nv.backend.{{ .Release.Namespace }}
spec:
  dlp:
    settings: []
    status: true
  egress: []
  file: []
  ingress: []
  process:
  - action: allow
    allow_update: false
    name: pause
    path: /pause
  - action: allow
    allow_update: false
    name: python3
    path: /usr/local/bin/python3.11
  process_profile:
    baseline: basic
  target:
    policymode: Protect
    selector:
      comment: ""
      criteria:
      - key: domain
        op: =
        value: {{ .Release.Namespace }}
      - key: service
        op: =
        value: backend.{{ .Release.Namespace }}
      name: nv.backend.{{ .Release.Namespace }}
      original_name: ""
  waf:
    settings: []
    status: true
{{- end }}
