defaultNamespace: demo

helm:
  chart: ./chart
  valuesFiles:
  - values.yaml

targetCustomizations:
- name: persistence
  clusterSelector:
    matchLabels:
      persistence: enabled
  helm:
    values:
      persistence:
        enabled: true
